# This database is created and reset by `npm run test:e2e` only after the
# `_test` suffix and separation from DATABASE_URL have been verified.
E2E_DATABASE_URL=postgresql://postgres:postgres@localhost:5433/better_content_e2e_test
